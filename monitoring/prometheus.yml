global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "spendlyserver"
    static_configs:
      # host.docker.internal resolves to your Mac's host from inside Docker
      - targets: ["host.docker.internal:3000"]
